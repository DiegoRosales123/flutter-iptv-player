allprojects {
    afterEvaluate { project ->
        if (project.hasProperty("android")) {
            android {
                compileOptions {
                    coreLibraryDesugaringEnabled = false
                }
            }
        }
    }
}
